  <!-- contenedor principal. todo debe ir dentro de estas etiquetas -->
<odoo>
  <data>
    <!-- explicit list view definition -->
  
  <!-- vista de la lista. aquí aparece todo lo que se defina en la tabla -->
    <record model="ir.ui.view" id="gestion_restaurante_valeria.list">
      <field name="name">Restaurante Valeria</field>
      <field name="model">gestion_restaurante_valeria.plato</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="description"/>
        </list>
      </field>
    </record>


    <!-- actions opening views on models -->
    <!-- la acción de la ventana - define qué hace -->

    <record model="ir.actions.act_window" id="gestion_restaurante_valeria.action_window">
      <field name="name">Gestión de Platos</field>
      <field name="res_model">gestion_restaurante_valeria.plato</field>
      <field name="view_mode">list,form</field>
    </record>


    <!-- server action to the one above -->

    <record model="ir.actions.server" id="gestion_restaurante_valeria.action_server">
      <field name="name">GestionRestauranteValeria server</field>
      <field name="model_id" ref="model_gestion_restaurante_valeria_plato"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>

<!-- botones del menú.-->
    <!-- Top menu item -->

    <menuitem name="Restaurante Valeria" id="gestion_restaurante_valeria.menu_root"/>
    <!-- submenu -->
    <menuitem name="Menú" id="gestion_restaurante_valeria.menu_main" parent="gestion_restaurante_valeria.menu_root"/>
    <!-- menu categories -->
     <menuitem name="Plato" id="gestion_restaurante_valeria.plato_list" 
     parent="gestion_restaurante_valeria.menu_main" 
     action="gestion_restaurante_valeria.action_window"/>
    
    
    <!-- actions -->
<!--
    <menuitem name="List" id="gestion_restaurante_valeria.menu_1_list" parent="gestion_restaurante_valeria.menu_1"
              action="gestion_restaurante_valeria.action_window"/>
    <menuitem name="Server to list" id="gestion_restaurante_valeria" parent="gestion_restaurante_valeria.menu_2"
              action="gestion_restaurante_valeria.action_server"/>
-->
  </data>
</odoo>
